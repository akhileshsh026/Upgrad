function login(loggedIn) {
    message = "Please login!";
    console.log(message);
    if (loggedIn) {
        name = "Srishti";
        let name;
        var message = "Welcome " + name + "!";
        console.log(message);
    }
 }
 
 login(true);